apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: fivoria
  labels:
    app: redis
    tier: cache
spec:
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: redis
    tier: cache
  type: ClusterIP
