apiVersion: v1
kind: Service
metadata:
  name: agent-api
  namespace: fivoria
  labels:
    app: agent-api
    tier: backend
spec:
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: agent-api
    tier: backend
  type: ClusterIP
