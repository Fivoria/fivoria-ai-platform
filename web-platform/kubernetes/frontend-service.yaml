apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: fivoria
  labels:
    app: frontend
    tier: frontend
spec:
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: frontend
    tier: frontend
  type: ClusterIP
